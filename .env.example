# Root environment file consumed by docker-compose.
# Copy to .env before running `docker compose up -d`.

POSTGRES_USER=soc
POSTGRES_PASSWORD=change-me
POSTGRES_DB=soc_analyst
POSTGRES_PORT=5432

REDIS_PORT=6379
